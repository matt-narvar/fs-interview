import styles from './App.module.css';

export const App = () => {
  return (
    <div className={styles.app}>
      <h1 className={styles.title}>Condition Editor</h1>
      {/* Build your condition editor sidebar here */}
    </div>
  );
};
